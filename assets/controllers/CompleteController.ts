module Foodstrap {
    "use strict";

    export class CompleteController {

        static $inject = [
            "$scope",
            "$routeParams",
            "$location",
            "$interval",
            "$timeout"
        ];

        constructor(){
           
        }
        

    }
}